import { TypeJobFilterPipe } from './type-job-filter.pipe';

describe('TypeJobFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeJobFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
