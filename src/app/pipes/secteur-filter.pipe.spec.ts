import { SecteurFilterPipe } from './secteur-filter.pipe';

describe('SecteurFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SecteurFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
