import {PostsFilterByTittrePipe} from './posts-filter-by-tittre.pipe';

describe('PostsFilterByTittrePipe', () => {
  it('create an instance', () => {
    const pipe = new PostsFilterByTittrePipe();
    expect(pipe).toBeTruthy();
  });
});
