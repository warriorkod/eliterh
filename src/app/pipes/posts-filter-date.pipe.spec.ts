import {PossFilterDatePipe} from './posts-filter-date.pipe';

describe('PossFilterDatePipe', () => {
  it('create an instance', () => {
    const pipe = new PossFilterDatePipe();
    expect(pipe).toBeTruthy();
  });
});
